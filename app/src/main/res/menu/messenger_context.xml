<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".ui.main.MainActivity"
    tools:ignore="AlwaysShowAction">

    <item
        android:id="@+id/action_message_share"
        android:enabled="false"
        android:icon="@drawable/ic_action_share"
        android:title="share with another app"
        android:visible="false"
        app:showAsAction="always" />

    <item
        android:id="@+id/action_message_resend"
        android:enabled="false"
        android:icon="@drawable/ic_action_resend"
        android:title="resend in Owfar"
        android:visible="false"
        app:showAsAction="always" />

    <item
        android:id="@+id/action_message_copy"
        android:icon="@drawable/ic_action_copy"
        android:title="copy text"
        app:showAsAction="always" />

    <item
        android:id="@+id/action_message_info"
        android:enabled="false"
        android:icon="@drawable/ic_action_info"
        android:title="message details"
        android:visible="false"
        app:showAsAction="always" />

    <item
        android:id="@+id/action_message_delete"
        android:icon="@drawable/ic_action_delete"
        android:title="retract message"
        app:showAsAction="always" />
</menu>
